/** @type {import('next-sitemap').IConfig} */
module.exports = {
  siteUrl: "https://nexobiiz.devlincon.com.br", // Substitua pelo URL do seu site
  generateRobotsTxt: true, // Gera o arquivo robots.txt
  sitemapSize: 7000,
  changefreq: "monthly",
  priority: 0.7,
};
